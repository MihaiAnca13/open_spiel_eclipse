You are monitoring a long reinforcement-learning training run that is ALREADY RUNNING.
Repo: /home/mihai/personal/open_spiel_eclipse. Work only inside that directory.

## What is running
`run_long_v2.sh` launched a PPO self-play training run for the board game Eclipse.
It writes to `runs/long_v2/` and stops itself after 12 hours (`--max_seconds=43200`).
At ~4900 envstep/s it should reach roughly 200M steps.

## YOUR JOB — three things, in this order

### 1. Monitor, every ~30 minutes, until the run stops
Run this and read the output:

    ./monitor_long_v2.sh

That is the ONLY command you need for monitoring. It is read-only.

Between ticks, sleep. Use: `sleep 1800`

Keep a running note of each tick's numbers. Expect roughly:
- state: RUNNING
- oom_lines: 0
- sps: ~4500-5200
- entropy: around 0.8-1.2 (it uses --ent_coef=0.05)
- ev (explained variance): should climb toward 0.6-0.8
- oob (returns out of band): should stay small, under ~0.05

### 2. STOP THE RUN EARLY only if one of these is true
- `oom_lines` is greater than 0  -> the run has crashed; stop monitoring, record it.
- `state: NOT RUNNING` before 12h have elapsed -> it died; record the last 30 lines of
  `runs/long_v2/train.log` (strip carriage returns: `tr '\r' '\n' < runs/long_v2/train.log | tail -30`).
- `sps` collapses below 1000 and stays there for two consecutive ticks -> something is wrong.

To stop it deliberately: `pkill -f "roster_dir=runs/long_v2"`

DO NOT stop it merely because the numbers look flat. Flatness is expected and is NOT
evidence of anything. See the warning in step 3.

### 3. When the run stops (or you stop it), judge it and write the report
Run the ladder. This plays the new snapshots head-to-head against the previous best
policies on fixed boards. It takes a while; let it finish:

    ./run_judge.sh 64 runs/wave_ladder3.json

Then append a section to `docs/NEXT_SESSION.md` titled
`## long_v2 result (<date>)` containing:
- total steps reached, wall-clock, and the average sps you observed
- the tick-by-tick summary you kept (a compact table is fine)
- the full ratings table from `runs/wave_ladder3.json`, printed with:

    .venv/bin/python -c "
    import json; d=json.load(open('runs/wave_ladder3.json'))
    for p in sorted(d['policies'], key=lambda p:-p['rating']):
        print(f\"{p['id']:30s} {p['rating']:+.4f} [{p['rating_ci'][0]:+.4f},{p['rating_ci'][1]:+.4f}] games={p['games']}\")
    "

- whether any `long_v2` policy beat the others, using ONLY this test: a policy wins if
  its `rating_ci` LOWER bound is above the other's `rating_ci` UPPER bound. If the CIs
  overlap, the correct conclusion is "no significant difference" — say exactly that.

## CRITICAL RULES — these were learned the hard way, do not deviate

1. **NEVER judge strength from `vp_all`, `mean_episode_return`, the `return_trend` hint,
   or any "beats Greedy" verdict.** Those metrics are decoupled from actual playing
   strength in this project, and reading them as strength has produced two wrong
   conclusions already. ONLY the ladder's `rating` / `rating_ci` counts.

2. **Ladder ratings are NOT comparable between different ladder runs.** The rating is fit
   relative to whichever policies were in that tournament. Only compare numbers inside a
   single `wave_ladder*.json`. Never say "1.10 now vs 1.22 before".

3. **Use `rating`, never the `elo` field.**

4. **Only ONE thing may use the GPU at a time.** Do not start any training or benchmark
   while the run is going. Do not run the ladder until the training run has stopped.

5. **Never write a wait-loop gated on `pgrep -f "<text that is also in your own command>"`**
   — the shell matches its own command line and loops forever. Poll for a FILE instead,
   or use `./monitor_long_v2.sh` and read `state:`.

6. Do not modify any Python source, do not `git commit`, do not delete anything under
   `runs/`. Your only write is appending the report section to `docs/NEXT_SESSION.md`.

7. If a command fails, report the real error. Do not claim success you did not observe.

Begin by running `./monitor_long_v2.sh` once to confirm the run is alive, then start the
30-minute monitoring cycle.
